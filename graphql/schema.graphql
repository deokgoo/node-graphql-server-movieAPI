type Movie {
  id: Int!
  title: String!
  original_title: String!
  popularity: Float!
  overview: String!
  language: String!
  poster_path: String!
  release_date: String!
  adult: Boolean!
}

type Query {
  ranking: [Movie]
  searchById(id: Int): Movie
  search(title: String!): String
}
